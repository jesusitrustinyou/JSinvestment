services:
  - type: web
    name: jsinvestments
    env: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: node server/index.js
    healthCheckPath: /health
    autoDeploy: true